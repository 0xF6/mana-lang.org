import { FeaturesList } from 'features-list'
import { Layout } from 'layout'
import React from 'react'

const Home = () => {
  return (
    <Layout>
      <FeaturesList />
    </Layout>
  )
}

export default Home
